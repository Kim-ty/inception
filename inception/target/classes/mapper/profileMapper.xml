<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.inception.mapper.profileMapper">

	<select id="showProfile" resultType="kr.co.inception.profile.vo.ProfileVO">
		<![CDATA[
			select u.userid,u.profilepicture,f.follow,f2.follower
			from tb_user u,
  				(select count(follow) as follow
  				 from userfollow
  				 where userid = #{userid,jdbcType=VARCHAR}) f,
  				(select count(follower) as follower
  				 from userfollower
  				 where userid = #{userid,jdbcType=VARCHAR}) f2
			where userid =#{userid,jdbcType=VARCHAR}
		]]>
	</select>
  
	<select id="showProfileBoardList" resultType="kr.co.inception.profile.vo.ProfileBoardListVO">
		<![CDATA[
			select title,CONTENTS,writedate
			from board
			where userid = #{userid}
		
		]]>	
	</select>  
  	
	<select id="showProfileReplyList" resultType="kr.co.inception.profile.vo.ProfileReplyListVO">
		<![CDATA[
			select b.title,r.CONTENTS
			from board b,boardreply r
			where b.bidx = r.bidx and r.userid= #{userid}

		
		]]>	
	</select>  
  	
	<select id="showProfileScrapeList" resultType="kr.co.inception.profile.vo.ProfileScrapeListVO">
		<![CDATA[
			select b.title,b.contents,b.userid
			from board b,scrape s
			where b.bidx = s.bidx and s.userid = #{userid}
		
		]]>	
	</select>  
  	
  
  	
  	
  	
	
  
</mapper>
  
  
  
  
  